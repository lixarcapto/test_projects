


from .index import*

class Docutil:

    def load_a_docx_file_as_text(route):
        return load_a_docx_file_as_text(route)

    def load_excel_file_as_array(route):
        return load_excel_file_as_array(route)

    def load_an_excel_as_key_value_dictionary(route):
        return load_an_excel_as_key_value_dictionary(route)

    def load_an_excel_file_as_a_string_array_dictionary(route):
        return load_an_excel_file_as_a_string_array_dictionary(\
            route)

    def load_a_list_of_excel_as_a_map_of_string_array_map(\
        direction_folder,
        direction_array
        ):
        return load_a_list_of_excel_as_a_map_of_string_array_map(\
            direction_folder,
            direction_array
            )

    # return text
    def load_a_docx_file_as_text_with_line_breaks(ruta_archivo):
        return load_a_docx_file_as_text_with_line_breaks(\
            ruta_archivo)

    # not return
    def save_text_inside_a_word_file(ruta_archivo, texto):
        return save_text_inside_a_word_file(\
            ruta_archivo,
            texto)

    # Not return
    def save_a_dictionary_of_string_arrays_in_an_excel(\
            diccionario,
            nombre_archivo
            ):
        return save_a_dictionary_of_string_arrays_in_an_excel(\
                diccionario,
                nombre_archivo
                )

    # return PhotoImage
    def load_image_as_photoimage(ruta_imagen):
        return load_image_as_photoimage(ruta_imagen)
